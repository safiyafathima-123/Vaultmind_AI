services:
  - type: web
    name: pl-genesis-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        value: https://pl-genesis-ai.vercel.app # Change this after deployment
